#include "main.h"

/**
* *string_toupper - changing to upper case
* @n: input value
* Return: always 0
*/

char *string_toupper(char *n)
{
       char *ptr = n;

	while (n[i] != '\0')
	{
		if (n[i] >= 'a' && n[i] <= 'z')
		{
			n[i] =;
		}
		i++;
	}
	return (0);
}
